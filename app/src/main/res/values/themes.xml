<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Trabapp" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/navyBlack</item>
        <item name="colorPrimaryDark">@color/navyBlack</item>
        <item name="colorAccent">@color/pointYellow</item>
        <item name="android:windowBackground">@color/white</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Trabapp" parent="Base.Theme.Trabapp" />

    <style name="ScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/screen_background</item>
    </style>
</resources>